{
  "name": "instagram",
  "version": "1.0.0",
"scripts": {
  "frontend": "npm start --prefix frontend",
  "backend": "npm start --prefix backend",
  "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
  "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider npm run build --prefix frontend"

},

  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
